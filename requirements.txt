fastapi>=0.110
uvicorn[standard]>=0.29
pdfplumber>=0.11
httpx>=0.27
python-multipart>=0.0.9
jinja2>=3.1
pyyaml>=6.0
python-dotenv>=1.0
# Optional: only needed for the direct-API backend (GM_BACKEND=api / ANTHROPIC_API_KEY).
# The Claude Code backend needs nothing here.
anthropic>=0.49
